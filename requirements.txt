# --- 台本生成（LLM） ---
anthropic>=0.40.0

# --- 音声合成（TTS） ---
gTTS>=2.5.1
elevenlabs>=1.9.0

# --- 動画・画像合成 ---
# moviepy 2.x は API が大きく変わっており（moviepy.editor の廃止など）、
# 本パイプラインは 1.x 系のAPIで書かれているため上限を固定している。
moviepy>=1.0.3,<2
imageio-ffmpeg>=0.5.1   # ffmpeg実行バイナリを同梱。システムにffmpegが無くても動作する保険
Pillow>=10.4.0
numpy>=1.26.0

# --- YouTube アップロード ---
google-api-python-client>=2.140.0
google-auth>=2.34.0
google-auth-oauthlib>=1.2.1

# --- 共通 ---
PyYAML>=6.0.2
python-dotenv>=1.0.1
